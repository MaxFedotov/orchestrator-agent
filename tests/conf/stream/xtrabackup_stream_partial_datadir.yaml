# xtrabackup_stream_partial_datadir.yaml

---

description:  |
  test parameters for xtrabackup stream partial to datadir seed method

variables:
  seedMethod: xtrabackup-stream
  backupFolder: '%2Fvar%2Flib%2Fmysql'
  databases: "sakila,world"